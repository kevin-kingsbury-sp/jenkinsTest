#!/usr/bin/env groovy

pipeline {
  agent { label 'linux' }
  stages {
    stage('build') {
      steps {
        echo 'Building...'
      }
    }

    stage('test') {
      steps {
        echo 'Testing...'
      }
    }
  }
}

